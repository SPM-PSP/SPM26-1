# 狼人杀系统前端单元测试报告

**项目名称**：多智能体在线语音狼人杀系统  
**测试范围**：游戏主页面前端（`client/src/`）· AI 复盘页面前端（`replay-frontend/src/`）  
**测试日期**：2026-05-31  
**测试人员**：杨策华

---

## 一、游戏主页面前端（`client/src/`）

### 1.1 用户认证（`api/login.js` · `helper/index.js`）

| 测试编号 | 测试内容 | 输入 | 预期结果 | 实际结果 |
|----------|----------|------|----------|----------|
| UT-C01 | 用户登录成功 | 正确用户名 + 正确密码 | 返回成功，写入 accessToken Cookie，跳转首页 | 通过 |
| UT-C02 | 用户登录失败（密码错误） | 正确用户名 + 错误密码 | 返回失败，弹出错误提示，停留登录页 | 通过 |
| UT-C03 | 用户注册成功 | 新用户名 + 昵称 + 密码 | 返回成功，自动跳转登录状态 | 通过 |
| UT-C04 | 已登录用户访问登录页 | 浏览器 Cookie 已有有效 token | 自动重定向至首页，不渲染登录表单 | 通过 |
| UT-C05 | 用户登出 | 点击登出按钮 | 清除 Cookie 中的 token，跳转登录页 | 通过 |
| UT-C06 | 权限检查（有权限） | `hasCPermission('system.admin')`，角色含该权限 | 返回 `true` | 通过 |
| UT-C07 | 权限检查（无权限） | `hasCPermission('system.admin')`，角色不含该权限 | 返回 `false` | 通过 |

---

### 1.2 房间管理（`api/room.js`）

| 测试编号 | 测试内容 | 输入 | 预期结果 | 实际结果 |
|----------|----------|------|----------|----------|
| UT-C08 | 创建房间 | 房间名 = "测试房间" | 返回新房间 ID 和加入密钥 | 通过 |
| UT-C09 | 加入房间（正确密钥） | roomKey = 有效房间密钥 | 进入房间，返回房间信息 | 通过 |
| UT-C10 | 加入房间（错误密钥） | roomKey = "INVALID" | 返回失败，弹出错误提示 | 通过 |
| UT-C11 | 玩家入座 | 座位号 = 3 | 玩家占用 3 号位，房间座位列表更新 | 通过 |
| UT-C12 | 踢出玩家（房主操作） | 目标玩家 userId | 目标玩家离开房间，房间刷新 | 通过 |

---

### 1.3 游戏流程（`api/game.js` · `pages/views/room/index.jsx`）

| 测试编号 | 测试内容 | 输入 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|----------|------|
| UT-C13 | 开始游戏（满足条件） | 房间内至少 1 名真人玩家，房主操作 | 游戏创建成功，进入夜晚阶段（stage=0） | 通过 | |
| UT-C14 | 开始游戏（无真人玩家） | 房间内全为 AI 玩家 | 前端校验失败，提示"至少需要 1 名真人玩家" | 通过 | `startSeatStats` 前端校验 |
| UT-C15 | 游戏阶段推进 | stage=0（夜晚），房主点击下一阶段 | stage 变为 1，页面 UI 切换至对应阶段 | 通过 | |
| UT-C16 | 玩家存活/出局状态判断 | 存活玩家 `status=1`，出局玩家 `status=0` | `isAlivePlayer()` 返回 `true`，`isOutPlayer()` 返回 `true` | 通过 | |
| UT-C17 | 游戏设置持久化 | 玩家数=10，女巫自救=首夜，胜利条件=屠边 | 设置写入 localStorage，刷新后可正常读取还原 | 通过 | |

---

### 1.4 行动系统（`pages/views/room/index.jsx`）

| 测试编号 | 测试内容 | 输入 | 预期结果 | 实际结果 |
|----------|----------|------|----------|----------|
| UT-C18 | 预言家查验目标筛选 | 行动类型=check | 过滤掉自身和已查验玩家，仅显示可查验目标 | 通过 |
| UT-C19 | 投票目标筛选 | 行动类型=vote | 出局玩家不出现在投票列表中 | 通过 |
| UT-C20 | 行动确认弹窗（女巫毒杀） | 行动类型=poison，选择目标玩家 | 弹出确认框，显示"确定对该玩家使用毒药吗？" | 通过 |
| UT-C21 | 行动后玩家状态更新 | 狼人袭击成功 | 被袭击玩家状态更新为 `status=0`（出局） | 通过 |

---

### 1.5 语音与 WebSocket（`api/voice.js` · `pages/views/room/index.jsx`）

| 测试编号 | 测试内容 | 输入 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|----------|------|
| UT-C22 | 语音转文字（STT） | 录音结束后上传音频 FormData | 返回识别后的文字内容 | 通过 | `multipart/form-data` |
| UT-C23 | 文字转语音（TTS） | AI 发言文字字符串 | 返回 Blob 格式音频数据 | 通过 | `responseType: blob` |
| UT-C24 | 语音作用域检测（狼人频道） | stage=2，玩家角色为狼人 | 只接收狼人频道语音，不播放给其他玩家 | 通过 | scope="wolf" |
| UT-C25 | WebSocket 接收阶段切换消息 | WebSocket 收到 type="stageChange" | 更新当前阶段，刷新游戏状态 | 通过 | |
| UT-C26 | WebSocket 接收游戏结束消息 | WebSocket 收到 type="gameOver" | 触发结算流程，显示结算页面 | 通过 | |

---

### 1.6 工具函数与 HTTP 拦截器（`common/utils.js` · `common/fetch.js`）

| 测试编号 | 测试内容 | 输入 | 预期结果 | 实际结果 |
|----------|----------|------|----------|----------|
| UT-C27 | 日期格式化 | `getCurrentDate(new Date("2026-05-31"))` | 返回 `"2026-05-31"` | 通过 |
| UT-C28 | 邮箱格式验证（合法） | `verifyEmailFormat("test@example.com")` | 返回 `true` | 通过 |
| UT-C29 | 邮箱格式验证（非法） | `verifyEmailFormat("notanemail")` | 返回 `false` | 通过 |
| UT-C30 | 接口返回 success=false | 后端返回 `{success:false, errorMessage:"错误"}` | 拦截器 reject，弹出全局错误提示 | 通过 |
| UT-C31 | 登录态失效（errorCode 4005） | 后端返回 errorCode=4005 | 弹出"登录失效"提示，自动跳转登录页 | 通过 |

---

## 二、AI 复盘页面前端（`replay-frontend/src/`）

### 2.1 数据加载（`data/reportAdapter.js` — `loadReport`）

| 测试编号 | 测试内容 | 输入 | 预期结果 | 实际结果 |
|----------|----------|------|----------|----------|
| UT-R01 | 从 URL 参数提取 Token | URL = `?gameId=123&token=abc` | token 存入 sessionStorage，URL 中 token 参数被移除 | 通过 |
| UT-R02 | 复盘详情 API 调用 | gameId="game_001" | 发起 GET `/api/game/replay/detail/auth?gameId=game_001`，携带 Authorization 头 | 通过 |
| UT-R03 | 加载状态管理 | 调用 `loadReport()` 直至完成 | 开始时 `isLoading=true`，完成后恢复 `false`（含异常情况） | 通过 |
| UT-R04 | 后端返回错误 | API 返回 `{success:false, errorMessage:"未授权"}` | `loadError.value = "未授权"`，reportData 不更新 | 通过 |
| UT-R05 | 无复盘内容时报错 | API 返回空 text 且无结构化 JSON | `loadError.value = "未找到复盘内容"` | 通过 |

---

### 2.2 Markdown 解析（`data/reportAdapter.js`）

| 测试编号 | 测试内容 | 输入 | 预期结果 | 实际结果 |
|----------|----------|------|----------|----------|
| UT-R06 | 提取"总览"小节 | Markdown 含 `## 总览\n内容文本` | `extractSections()` 返回 `{summary: "内容文本"}` | 通过 |
| UT-R07 | 中文别名映射 | 标题分别为"票型分析""发言问题""技能评估" | 分别映射为内部键 `votes`、`speech`、`skills` | 通过 |
| UT-R08 | JSON 代码块提取（合法） | ` ```json\n{"key":"val"}\n``` ` | `extractFencedJson()` 返回 `{key:"val"}` | 通过 |
| UT-R09 | JSON 代码块提取（非法） | ` ```json\n{broken json\n``` ` | 返回 `null`，不抛出异常 | 通过 |
| UT-R10 | 段落分割 | `splitParagraphs("段落一\n\n段落二")` | 返回 `["段落一", "段落二"]` | 通过 |
| UT-R11 | 列表行解析 | `listLines("- 项目1\n* 项目2")` | 返回 `["项目1", "项目2"]`（去除前缀符号） | 通过 |

---

### 2.3 数据转换与报告构建（`data/reportAdapter.js`）

| 测试编号 | 测试内容 | 输入 | 预期结果 | 实际结果 |
|----------|----------|------|----------|----------|
| UT-R12 | 投票轮次解析 | `vote_analysis:{round_1:{vote_counts:{p1:3,p2:1},voted_out:"Player1"}}` | 返回 `[{round:"第 1 轮投票", headline:"Player1 被投出局", players:["Player1 3票","Player2 1票"]}]` | 通过 |
| UT-R13 | 票数文本提取（含票） | `"Player1获3票，Player2获1票"` | 返回 `["Player1获3票", "Player2获1票"]` | 通过 |
| UT-R14 | 票数文本提取（弃票） | `"本轮弃票，无人被放逐"` | 返回 `[]` | 通过 |
| UT-R15 | 技能标题规范化 | `normalizeSkillTitle("Player3，预言家")` | 返回 `"预言家（Player3）"` | 通过 |
| UT-R16 | 从摘要提取胜利阵营 | summaryText = `"好人阵营最终获胜..."` | `getWinningCamp()` 返回 `"好人阵营获胜"` | 通过 |
| UT-R17 | 总览面板构建 | 2 轮投票、3 项发言问题、含女巫技能 | 返回 4 项指标：胜利阵营 / 票型2轮 / 发言3项 / 关键技能女巫 | 通过 |
| UT-R18 | 结构化数据优先于 Markdown | replayText 同时含 ` ```json {...} ``` ` 和 Markdown 文本 | 各子模块优先走结构化解析路径 | 通过 |
| UT-R19 | 完整报告对象字段完整性 | 正常调用 `buildReportData()` | 返回对象含 meta、summaryParagraphs、overview、voteRounds、speechIssues、skillEvaluations、mistakes、strategyRecommendations、heroTags | 通过 |

---

### 2.4 App.vue 页面组件（`replay-frontend/src/App.vue`）

| 测试编号 | 测试内容 | 输入 | 预期结果 | 实际结果 |
|----------|----------|------|----------|----------|
| UT-R20 | 挂载后自动调用 loadReport | URL = `?gameId=game_001` | `onMounted` 提取 gameId 并调用 `loadReport("game_001")` | 通过 |
| UT-R21 | 加载中显示占位内容 | `isLoading.value = true` | 显示 loading 占位，不渲染报告区块 | 通过 |
| UT-R22 | 加载失败显示错误提示 | `loadError.value = "复盘分析失败"` | 页面显示"复盘分析失败"错误文字 | 通过 |
| UT-R23 | 返回按钮行为 | 点击返回，`history.length > 1` / `<= 1` | 有历史记录时 `history.back()`，否则 `window.close()` | 通过 |

---

## 三、测试汇总

| 模块 | 测试用例数 | 通过 | 失败 | 通过率 |
|------|-----------|------|------|--------|
| 用户认证（client） | 7 | 7 | 0 | 100% |
| 房间管理（client） | 5 | 5 | 0 | 100% |
| 游戏流程（client） | 5 | 5 | 0 | 100% |
| 行动系统（client） | 4 | 4 | 0 | 100% |
| 语音与 WebSocket（client） | 5 | 5 | 0 | 100% |
| 工具函数与拦截器（client） | 5 | 5 | 0 | 100% |
| 数据加载（replay） | 5 | 5 | 0 | 100% |
| Markdown 解析（replay） | 6 | 6 | 0 | 100% |
| 数据转换与报告构建（replay） | 8 | 8 | 0 | 100% |
| App.vue 组件（replay） | 4 | 4 | 0 | 100% |
| **合计** | **54** | **54** | **0** | **100%** |
